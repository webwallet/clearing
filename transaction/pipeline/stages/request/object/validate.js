'use strict'

function validateTransactionRequestObject(parameters = {}) {
  let request = parameters.transaction.request
  // JOI validation:

  return parameters
}

module.exports = validateTransactionRequestObject
